# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
        env:
          VITE_ASSISTANT_PUBLIC_KEY: ${{ secrets.VITE_ASSISTANT_PUBLIC_KEY }}
          VITE_ENGLISH_ASSISTANT_ID: ${{ secrets.VITE_ENGLISH_ASSISTANT_ID }}
          VITE_ROMANIAN_ASSISTANT_ID: ${{ secrets.VITE_ROMANIAN_ASSISTANT_ID }}
          VITE_RUSSIAN_ASSISTANT_ID: ${{ secrets.VITE_RUSSIAN_ASSISTANT_ID }}
          VITE_ENGLISH_LANGUAGE: ${{ secrets.VITE_ENGLISH_LANGUAGE }}
          VITE_RUSSIAN_LANGUAGE: ${{ secrets.VITE_RUSSIAN_LANGUAGE }}
          VITE_ROMANIAN_LANGUAGE: ${{ secrets.VITE_ROMANIAN_LANGUAGE }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PANADERO_8BE5C }}
          projectId: panadero-8be5c
